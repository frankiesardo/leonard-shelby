---
layout: base.njk
---
<article class="note">
  <h1>{{ title or page.fileSlug }}</h1>

  {% if date %}<time>{{ date }}</time>{% endif %}

  {% if domains %}
  <p class="domains">{% for d in domains %}<span class="domain">{{ d }}</span>{% if not loop.last %} {% endif %}{% endfor %}</p>
  {% endif %}

  <div class="content">
    {{ content | safe }}
  </div>
</article>
